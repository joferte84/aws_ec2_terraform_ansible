<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Servidor Web Personalizado</title>
    <style>
        body {
            font-family: 'Helvetica Neue', sans-serif;
            background: linear-gradient(135deg, #74ebd5, #9face6);
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        .container {
            background-color: rgba(255, 255, 255, 0.85);
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 15px 25px rgba(0, 0, 0, 0.2);
            text-align: center;
            transition: transform 0.3s ease-in-out;
        }
        .container:hover {
            transform: scale(1.05);
        }
        h1 {
            color: #333;
            font-size: 2.5rem;
        }
        p {
            color: #555;
            font-size: 1.2rem;
        }
        .hostname, .ip-address {
            font-size: 1.1rem;
            color: #222;
            background-color: rgba(0, 0, 0, 0.05);
            padding: 10px;
            border-radius: 10px;
            margin-top: 10px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>¡Bienvenido a tu servidor personalizado!</h1>
        <p>{{ custom_message }}</p>
        <div class="hostname">
            <strong>Nombre del servidor:</strong> {{ ansible_hostname }}
        </div>
        <div class="ip-address">
            <strong>Dirección IP:</strong> {{ ansible_default_ipv4.address }}
        </div>
        <p>Este servidor fue configurado automáticamente por Ansible.</p>
    </div>
</body>
</html>
